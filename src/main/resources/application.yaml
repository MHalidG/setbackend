spring:

  security:
    jwt:
      secret: ${JWT_SECRET}

  datasource:
    url: jdbc:postgresql://localhost:5432/setdb
    username: halit
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # DEV için
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  jackson:
    serialization:
      indent_output: true

server:
  port: 8080
